--!strict
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

-- Remote function
local SpawnBrainrot = ReplicatedStorage:WaitForChild("AdminService"):WaitForChild("SpawnBrainrot")

-- Remove old GUI if exists
if PlayerGui:FindFirstChild("BrainrotSpawnerGUI") then
    PlayerGui.BrainrotSpawnerGUI:Destroy()
end

-- ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BrainrotSpawnerGUI"
ScreenGui.Parent = PlayerGui

-- Main Frame
local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 250, 0, 180)
Frame.Position = UDim2.new(0.5, -125, 0.5, -90)
Frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Frame.BorderSizePixel = 0
Frame.Parent = ScreenGui
Frame.Active = true
Frame.Draggable = true

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 25)
Title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextScaled = true
Title.Text = "Brainrot Spawner"
Title.Parent = Frame

-- Name Box
local NameBox = Instance.new("TextBox")
NameBox.Size = UDim2.new(0.9, 0, 0, 25)
NameBox.Position = UDim2.new(0.05, 0, 0, 35)
NameBox.PlaceholderText = "Brainrot Name"
NameBox.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
NameBox.TextColor3 = Color3.fromRGB(255, 255, 255)
NameBox.Parent = Frame

-- Mutation Box
local MutationBox = Instance.new("TextBox")
MutationBox.Size = UDim2.new(0.9, 0, 0, 25)
MutationBox.Position = UDim2.new(0.05, 0, 0, 65)
MutationBox.PlaceholderText = "Mutation (optional)"
MutationBox.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
MutationBox.TextColor3 = Color3.fromRGB(255, 255, 255)
MutationBox.Parent = Frame

-- Traits Box
local TraitsBox = Instance.new("TextBox")
TraitsBox.Size = UDim2.new(0.9, 0, 0, 25)
TraitsBox.Position = UDim2.new(0.05, 0, 0, 95)
TraitsBox.PlaceholderText = "Traits (comma-separated)"
TraitsBox.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
TraitsBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TraitsBox.Parent = Frame

-- Spawn Button
local SpawnButton = Instance.new("TextButton")
SpawnButton.Size = UDim2.new(0.9, 0, 0, 30)
SpawnButton.Position = UDim2.new(0.05, 0, 0, 130)
SpawnButton.BackgroundColor3 = Color3.fromRGB(120, 50, 50)
SpawnButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SpawnButton.Text = "Spawn Brainrot"
SpawnButton.Parent = Frame

-- Button click logic
SpawnButton.MouseButton1Click:Connect(function()
	local Name = NameBox.Text
	local Mutation = MutationBox.Text
	local Traits = TraitsBox.Text

	if Name == "" then
		warn("Please enter a Brainrot name!")
		return
	end

	if Traits == "" then
		Traits = "None"
	end

	local success, message = pcall(function()
		return SpawnBrainrot:InvokeServer(Name, Mutation, Traits, 20)
	end)

	if success then
		print("Brainrot spawned successfully!")
	else
		warn("Failed to spawn Brainrot:", message)
	end
end)
